type cardItemCategories = {
  name: string;
  cardNum: number;
  subText: string;
};

type darkModeButtonClassType = {
  mode: string;
  item: string;
};

export { cardItemCategories, darkModeButtonClassType };
